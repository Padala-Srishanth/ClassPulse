# =============================================================================
# ClassPulse API — Python Dependencies
# Phase 1: Server Foundation
#
# ML libraries (pandas, scikit-learn, numpy, pyod) are intentionally
# excluded here and will be added in Phase 3.
#
# NOTE: Version pins use >= to allow pip to select a pre-built wheel.
# Exact pinning should be done with `pip freeze > requirements-lock.txt`
# after a successful install.
# =============================================================================

# --- Web Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- Data Validation & Settings ---
# Using >= so pip can select a wheel that supports the installed Python version.
# pydantic-core requires Rust to compile from source if no wheel exists.
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- Firebase ---
firebase-admin>=6.5.0

# --- Environment ---
python-dotenv>=1.0.0

# --- HTTP Client (used by Firebase Admin SDK internally; also used in tests) ---
httpx>=0.27.0
# httpx2 is required by starlette 1.6+ TestClient for testing
httpx2>=0.27.0

# --- Testing ---
pytest>=8.3.0
pytest-asyncio>=0.24.0
